# 사진(이미지) 등록 가이드

`src/lib/content.ts`로 글자 내용을 수정하는 것처럼, 사진도 정해진 규칙에 따라 넣으면 사이트에 바로 반영됩니다. 이 문서는 사진을 "어디에 저장하고", "어떤 크기로 준비하고", "사이트에 어떻게 연결하는지"를 설명합니다.

## 1. 사진 저장 위치

사진 파일은 `public/images/` 폴더 아래, 용도별 폴더에 저장합니다. (`public` 폴더에 넣은 파일은 그대로 웹 주소 `/`로 시작하는 경로에서 접근할 수 있습니다.)

```
public/
  images/
    therapists/   ← We재활사 프로필 사진
    rooms/         ← We센터 공간(치료실, 대기실 등) 사진
    location/      ← 오시는길 사진
    posts/         ← 공지사항 / We재활생각 / 기타 게시글 첨부 사진
```

폴더가 없다면 새로 만들어서 사용하면 됩니다.

## 2. 파일명 규칙

- 영문 소문자 + 하이픈(`-`)만 사용 (띄어쓰기, 한글, 특수문자 금지)
- 무엇인지 알아볼 수 있는 이름 사용

예시: `kim-jihyun.jpg`, `susaek-waiting-room.jpg`, `notice-summer-break.jpg`

## 3. 용도별 권장 사진 크기

| 사용 위치 | 화면 비율 | 권장 최소 크기 | 비고 |
|---|---|---|---|
| We재활사 프로필 사진 | 정사각형 (1:1) | 500 × 500px | 원형으로 잘려서 보입니다. 얼굴이 가운데 오도록 촬영/크롭 |
| We센터 공간 사진 (치료실 등) | 4:3 | 1200 × 900px | 그리드에서 가로로 살짝 크게 보임 |
| 오시는길 사진 | 4:3 | 1200 × 900px | |
| 공지사항 / We재활생각 / 기타 게시글 사진 | 16:9 | 1600 × 900px | 게시글 상세 화면 상단에 크게 표시 |

## 4. 파일 형식 / 용량

- 형식: `.jpg` 또는 `.webp` 권장 (`.png`는 사진에는 용량이 커서 비권장)
- 용량: 장당 500KB 이하로 압축 권장
- 압축 도구 예시: [squoosh.app](https://squoosh.app), [tinypng.com](https://tinypng.com)

## 5. 사이트에 실제로 연결하는 방법

이 사이트는 관리자 모드에서 "이미지 URL" 입력칸에 경로를 입력하는 방식으로 사진을 등록합니다.

1. 위 크기/용량 기준에 맞게 사진을 준비합니다.
2. 준비한 파일을 해당 용도 폴더(`public/images/therapists/` 등)에 복사합니다.
3. 관리자로 로그인 후, 해당 항목의 "수정" 또는 "추가"를 누릅니다.
4. "사진 URL" / "이미지 URL" 입력칸에 `/images/therapists/kim-jihyun.jpg` 처럼 **폴더 경로 + 파일명**을 입력합니다.
5. 저장하면 바로 사진이 반영됩니다.

## 6. 주의: 저장 방식 차이

- 위 4~5번 방법(`public/images/...`에 파일을 넣고 경로를 입력하는 방법)은 사이트를 방문하는 **모든 사람에게 동일하게** 보입니다. 실제 서비스용 사진은 반드시 이 방법을 사용하세요.
- 오시는길 페이지의 "파일 업로드" 버튼처럼 파일을 직접 선택해서 올리는 기능은 사진을 **현재 사용 중인 브라우저에만** 저장합니다(다른 방문자·다른 기기에는 보이지 않음). 빠른 미리보기용으로만 사용하고, 실제 게시용 사진은 5번 방법으로 등록해 주세요.
